# ✅ Step 1: Node 환경 (빌드)
FROM node:22-alpine AS build

# 작업 디렉토리 생성
WORKDIR /app

# 패키지 복사 및 설치
COPY ./test/package*.json ./
RUN npm install

# 소스 복사 및 빌드
COPY ./test .
RUN npm run build

# ✅ Step 2: Nginx로 정적 파일 서빙
FROM nginx:alpine

# 빌드된 dist를 Nginx 기본 경로로 복사
COPY --from=build /app/dist /usr/share/nginx/html

# ✅ 포트 설정 (Koyeb Health check 대응)
EXPOSE 80

# ✅ Nginx 실행
CMD ["nginx", "-g", "daemon off;"]
